# Stable Node — Гайд по установке

## Содержание

> [Подготовка к установке ноды](#подготовка-к-установке-ноды)

> [Разбор требований ноды](#разбор-требований-ноды)

> [Установка ноды](#установка-ноды)

> [Бэкап ключей](#бэкап-ключей)

> [Полезные команды](#полезные-команды)

---

## Подготовка к установке ноды

- Убедитесь, что у вас есть сервер на Ubuntu/Debian с правами root
- Скрипт автоматически настроит окружение и откроет необходимые порты

> 💡 **Важно:** Скрипт автоматически откроет порт **26656** (P2P) в файрволе, если установлен UFW

---

## Разбор требований ноды

**Рекомендации проекта:**

- **CPU:** 2+ ядра
- **RAM:** 4+ GB
- **Диск:** 100+ GB SSD
- **ОС:** Ubuntu 20.04+ / Debian 11+

> ⚠️ **Важно!**
> 
> Это минимальные требования. При высокой нагрузке потребление ресурсов может увеличиться, поэтому не забивайте сервер по максимуму

**Порты, которые использует нода:**

- **26656** (TCP/UDP) — P2P порт для соединений с другими нодами
- **26657** (TCP) — RPC порт для запросов
- **26658** (TCP) — Proxy App порт (внутренний)
- **8545** (TCP) — Ethereum RPC API
- **8546** (TCP) — Ethereum WebSocket API
- **6060** (TCP) — pprof порт (только localhost)

---

## Установка ноды

- Перейдите в наш [**бот**](https://t.me/ProfitNodes_bot) и введите команду:

```bash
/get_script stable_node.sh
```

- В ответ вы получите сообщение с командой установки (выделено красным квадратом). **Для копирования этой команды просто нажмите на неё и вставьте в терминал, после нажмите ENTER**

> ⚠️ **Убеждаемся что язык клавиатуры включен EN (английский)**

- Вводим цифру **1 (Инициализация окружения сервера)** и нажимаем ENTER

    *Скрипт обновит систему и установит необходимые зависимости*

- Затем вводим цифру **2 (Развернуть ноду Stable)** и нажимаем ENTER

- Вводим **уникальное имя ноды (moniker)**:
    - Используйте только латинские буквы, цифры, дефис (-) и подчеркивание (_)
    - Имя должно быть уникальным
    - Пример: `MyStableNode_01` или `stable-node-validator`

- Скрипт спросит: **"Подтянуть снапшот сейчас? [y/N]:"**
    - Рекомендуется ответить **`y`** для быстрой синхронизации
    - Если ответите **`n`**, нода будет синхронизироваться с нуля (займёт больше времени)

- После завершения установки нода автоматически запустится

- Проверяем статус ноды командой:

```bash
systemctl status stabled
```

Или через скрипт: введите **5 (Информация о работе ноды)**

---

## Бэкап ключей

- Вызываем скрипт командой:

```bash
./stable_node.sh
```

- Выбираем пункт **16 (Архивация критических данных)**

- Скрипт создаст архив с критически важными файлами:
    - `priv_validator_key.json` — ключ валидатора
    - `node_key.json` — ключ ноды
    - `priv_validator_state.json` — состояние валидатора
    - `config.toml` и `app.toml` — конфигурационные файлы
    - `addrbook.json` — адресная книга

- Архив будет сохранён в `/root/stable_backups/`

> 🔐 **Важно:** Сохраните этот архив в надёжное место! Без этих ключей вы не сможете восстановить доступ к ноде

---

## Полезные команды

### Работа со скриптом

Вызываем скрипт командой:

```bash
./stable_node.sh
```

**Основные пункты меню:**

1. **📦 Инициализация окружения сервера** — обновление системы и установка зависимостей
2. **⚙️ Развернуть ноду Stable** — установка ноды
3. **🚀 Старт службы ноды** — запуск ноды
4. **📜 Просмотр журнала событий** — просмотр логов в реальном времени
5. **📊 Информация о работе ноды** — статус ноды
6. **🔄 Перезапуск службы** — перезапуск ноды
7. **🗑️ Полное удаление ноды** — удаление ноды и всех данных
8. **🏷️ Показать версию бинаря** — версия установленного бинаря
9. **💚 Проверка состояния (Health check)** — интерактивный мониторинг
10. **📸 Быстрая синхронизация (snapshot)** — применение снапшота
11. **⏰ Планировщик автосинхронизации** — настройка автоматического применения снапшотов
12. **🌐 Актуализация списка пиров** — обновление списка пиров
13. **🤖 Автообнаружение обновлений** — автоматическое обнаружение новой версии
14. **⬆️ Установить целевую версию** — ручное обновление до указанной версии
15. **⬇️ Восстановить предыдущую версию** — откат к предыдущей версии
16. **💾 Архивация критических данных** — создание бэкапа ключей
17. **🔧 Изменить порты ноды** — изменение портов ноды
0. **❌ Завершить работу** — выход из скрипта

### Прямые команды (без скрипта)

**Проверка статуса ноды:**
```bash
systemctl status stabled
```

**Просмотр логов:**
```bash
journalctl -u stabled -f -n 200
```

**Проверка синхронизации:**
```bash
curl -s localhost:26657/status | jq .result.sync_info.catching_up
```

**Проверка количества пиров:**
```bash
curl -s localhost:26657/net_info | jq .result.n_peers
```

**Проверка высоты блока:**
```bash
curl -s localhost:26657/status | jq .result.sync_info.latest_block_height
```

**Перезапуск ноды:**
```bash
systemctl restart stabled
```

**Остановка ноды:**
```bash
systemctl stop stabled
```

**Запуск ноды:**
```bash
systemctl start stabled
```

---

**Chain ID:** `stabletestnet_2201-1`

**Приватное сообщество о нодах Profit Nodes:** [https://t.me/ProfitNodes_bot](https://t.me/ProfitNodes_bot)

